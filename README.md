This project, part of my Computer Engineering thesis at Sapienza University of Rome, implements a custom block-based file system with file and directory management. It uses File Control Blocks (FCBs) to store metadata, supports standard file operations (create, read, write, delete) and directory navigation. The repository includes all source code, headers, and a Makefile for compilation and testing.
